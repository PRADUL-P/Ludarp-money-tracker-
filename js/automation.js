'use strict';
/* automation.js - optional automation hooks. Keep small for now. */

(function(){
  // placeholder for scheduled export, reminders, etc.
  window.MT = window.MT || {};
  window.MT.automation = {
    // add methods later: scheduleBackup(), remindToLog(), autoCategorize(), etc.
  };
})();
